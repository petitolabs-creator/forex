apiVersion: v1
kind: Service
metadata:
  name: one-frame
  namespace: forex
  labels:
    app: one-frame
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: one-frame
