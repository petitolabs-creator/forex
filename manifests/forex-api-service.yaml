apiVersion: v1
kind: Service
metadata:
  name: forex-api
  namespace: forex
  labels:
    app: forex-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: forex-api
